# Percentage at which CPU warning fires
cpu.warn=80

# Percentage at which RAM warning fires
ram.warn=85

# Free disk space (GB) below which a warning fires
disk.warn.gb=15

# How often the dashboard refreshes (seconds)
refresh.seconds=1

# Log file name (saved next to the JAR)
log.file=system_monitor_logs.txt